{
  "name": "pre-deploy-confirm",
  "type": "pipeline",
  "version": "1.0.0",
  "description": "Pre-deploy confirmation gate — presents deployment summary, waits for explicit user approval",
  "author": "Brett Hamlin",
  "repository": "https://github.com/BrettHamlin/collab-registry",
  "dependencies": [],
  "cliDependencies": [
    { "name": "bun", "version": ">=1.0.0", "required": true, "installHint": "curl -fsSL https://bun.sh/install | bash" }
  ],
  "commands": ["commands/collab.pre-deploy-confirm.md"],
  "handlers": ["handlers/emit-pre-deploy-confirm-signal.ts"],
  "executors": ["executors/pre-deploy-summary.ts"],
  "checksum": "pre-deploy-confirm-checksum"
}
