# @templates Mind Profile

## Domain

Placeholder Mind. The collab pipeline templates have been removed. The Minds architecture installs directly from the Mind source directories via `installCoreMinds()` — no intermediate template directory is needed.

## Status

This directory contains only `server.ts` (the Mind server stub). No distributable templates live here.
